<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:color">@color/toolBarTextColor</item>
        <item name="android:textColorSecondary">@color/toolBarTextColor</item>
        <item name="android:background">@color/appBackground</item>
    </style>

<!--activity-->
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

<!--appBar-->
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="appBarL">
        <item name="colorPrimary">@color/appBarPrimaryColor</item>
    </style>

<!--toolbar-->
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"></style>

    <style name="ToolBarStyle">
        <item name="android:textColorPrimary">@color/toolBarTextColor</item>
        <item name="android:background">@color/ToolBarBackgroundColor</item>

    </style>

    <style name="buttonStyle">
        <item name="android:background">@drawable/cut_shadow</item>
    </style>



</resources>
